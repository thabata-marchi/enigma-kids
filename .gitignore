# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
ehthumbs.db

# Node (if tooling is ever added)
node_modules/
npm-debug.log*

# AI model files — kept in version control intentionally (~1.7 MB total).
# The game needs them to run offline. Uncomment below ONLY if you switch
# to hosting the model on an external URL instead of this repo.
# model/*.bin
# model/*.json
# model/metadata.json
